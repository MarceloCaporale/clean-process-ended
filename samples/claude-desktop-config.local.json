{
  "mcpServers": {
    "clean-process-ended": {
      "command": "npx",
      "args": ["-y", "--package", "clean-process-ended", "clean-process-ended-mcp"],
      "env": {
        "CPE_HOST_PROFILE": "claude_code"
      }
    }
  }
}
